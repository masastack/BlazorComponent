@namespace BlazorComponent
@typeparam TComponent
@inherits ComponentAbstractBase<TComponent>

@if (Loading is not null && Loading != false)
{
    if (ProgressContent != null)
    {
        @ProgressContent
    }
    else
    {
        <AbstractComponent Metadata="AbstractProvider.GetMetadata<BProgressLinear>()"></AbstractComponent>
    }
}

