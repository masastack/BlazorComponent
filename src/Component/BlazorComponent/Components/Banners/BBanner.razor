@namespace BlazorComponent
@inherits BDomComponentBase

<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(CascadingValue<>))">
    <ExpandTransition>
        <Element Show="Value" class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()">
            <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BBannerWrapper<>))"></AbstractComponent>
        </Element>
    </ExpandTransition>
</AbstractComponent>