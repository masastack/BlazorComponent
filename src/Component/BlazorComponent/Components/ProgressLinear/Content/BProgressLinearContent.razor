@namespace BlazorComponent
@typeparam TProgressLinear
@inherits ComponentAbstractBase<TProgressLinear>

@if (ComponentChildContent != null)
{
	<div class="@CssProvider.GetClass("content")">
		@ComponentChildContent(Value)
	</div>
}

