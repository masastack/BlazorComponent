@namespace BlazorComponent
@inherits BDomComponentBase

@if (Plain)
{
    @((MarkupString)_mdHtml)
}
else
{
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" @attributes="@Attributes" @ref="Ref">
        @((MarkupString)_mdHtml)
    </div>
}
