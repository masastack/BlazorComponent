@namespace BlazorComponent
@typeparam TItem
@typeparam TItemValue
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

@{
    var props = new SelectSelectionProps<TItem>(Item, Index, Selected, Disabled);
    @SelectionContent?.Invoke(props)
}
