@namespace BlazorComponent
@typeparam TItem
@typeparam TItemValue
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

<BChip @attributes="GetAttributes(typeof(BChip),data:(Item,Index))">
    @GetText(Item)
</BChip>
