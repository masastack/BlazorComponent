@namespace BlazorComponent
@typeparam TItem
@typeparam TItemValue
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

<BMenu @attributes="GetAttributes(typeof(BMenu))" @ref="Menu">
    @if (MenuContent is not null)
    {
        @MenuContent
    }
    else
    {
        @RenderPart(typeof(BSelectList<,,,>))
    }
</BMenu>
