@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">
    <main class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref" @attributes="Attributes">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BMainWrap<>))"></AbstractComponent>
    </main>
</BCascadingValue>