@namespace BlazorComponent
@inherits BDomComponentBase
@typeparam TColumn
@typeparam TColumnItem
@typeparam TColumnItemValue

<div class="@CssProvider.GetClass("column")" style="@CssProvider.GetStyle("column")"
     @ontouchstart="OnTouchstart"
     @ontouchmove="OnTouchmove"
     @ontouchend="OnTouchend"
     @ontouchcancel="OnTouchend">
    <ul class="@CssProvider.GetClass("column-wrapper")" style="@CssProvider.GetStyle("column-wrapper")"
        @ontransitionend="OnTransitionEnd"
        @ref="Wrapper">
        @foreach (var (item, index) in Items.Select((item, index) => (item, index)))
        {
            <li class="@CssProvider.GetClass("column-item")" style="@CssProvider.GetStyle("column-item")" @onclick="() => OnClickItem(index)" @key="item">
                <div class="@CssProvider.GetClass("column-text")">@ItemText?.Invoke(item) Value:@SelectedIndex index:@index</div>
            </li>
        }
    </ul>
</div>
