@namespace BlazorComponent
@inherits BDomComponentBase

<div class="@CssProvider.GetClass()">
    @RenderPart(typeof(BTimePickerTitleTime<>))
    @if (AmPm)
    {
        @RenderPart(typeof(BTimePickerTitleAmPm<>))
    }
</div>

