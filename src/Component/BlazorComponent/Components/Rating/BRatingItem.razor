@namespace BlazorComponent
@typeparam TRating
@inherits ComponentPartBase<TRating>

@if (ItemContent != null)
{
    @ItemContent(Item)
}
else
{
    <BIcon @attributes="GetAttributes(typeof(BIcon),ItemIndex)">
        @GetIconName(Item)
    </BIcon>
}