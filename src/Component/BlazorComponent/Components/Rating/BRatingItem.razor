@namespace BlazorComponent
@typeparam TRating
@inherits ComponentAbstractBase<TRating>

@if (ItemContent != null)
{
    @ItemContent(Item)
}
else
{
    <AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BIcon>(ItemIndex))">
        @GetIconName(Item)
    </AbstractComponent>
}