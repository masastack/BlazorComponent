@namespace BlazorComponent
@typeparam TValue
@typeparam TInput
@inherits ComponentAbstractBase<TInput>
@*//TODO:这里还需要进一步完善*@
<div class="@CssProvider.GetClass(Type)">
    <AbstractComponent @onclick="OnClick" Metadata="AbstractProvider.GetMetadata<BIcon>(Type)">
        @Icon
    </AbstractComponent>
</div>
