@namespace BlazorComponent
@inherits BDomComponentBase
@typeparam TValue
@implements IInput<TValue>

<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(CascadingValue<>))">
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BInputContent<,>))">
        </AbstractComponent>
    </div>
</AbstractComponent>
