@namespace BlazorComponent
@typeparam TChip
@inherits ComponentPartBase<TChip>

@if (Filter)
{
    <ExpandXTransition>
        <ShowTransitionElement Value="IsActive">
            @* TODO: may need an AbstractComponentElement? *@
            <BIcon @attributes="@(GetAttributes(typeof(BIcon),"filter"))">
                @FilterIcon
            </BIcon>
        </ShowTransitionElement>
    </ExpandXTransition>
}