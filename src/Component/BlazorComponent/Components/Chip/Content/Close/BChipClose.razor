@namespace BlazorComponent
@typeparam TChip
@inherits ComponentPartBase<TChip>

@if (Close)
{
    <BIcon OnClickPreventDefault
           OnClickStopPropagation
           @attributes="@(GetAttributes(typeof(BIcon), "close"))">
        @CloseIcon
    </BIcon>
}
