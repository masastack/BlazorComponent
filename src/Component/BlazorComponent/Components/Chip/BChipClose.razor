@namespace BlazorComponent
@typeparam TChip
@inherits ComponentAbstractBase<TChip>

@if (Close)
{
    <AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BIcon>("close"))">
        @CloseIcon
    </AbstractComponent>
}
