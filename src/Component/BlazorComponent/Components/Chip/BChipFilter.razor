@namespace BlazorComponent
@typeparam TChip
@inherits ComponentAbstractBase<TChip>

@if (Filter)
{
    <ExpandXTransition>
        <Element Show="IsActive"> @* TODO: may need an AbstractComponentElement? *@
            <AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BIcon>("filter"))">
                @FilterIcon
            </AbstractComponent>
        </Element>
    </ExpandXTransition>
}