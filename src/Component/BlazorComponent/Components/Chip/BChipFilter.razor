@namespace BlazorComponent
@typeparam TChip
@inherits ComponentAbstractBase<TChip>

@if (Filter)
{
    @*<ExpandTransition X>*@
    @if (IsActive)
    {
        <AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BIcon>("filter"))">
            @FilterIcon
        </AbstractComponent>
    }
    @*</ExpandTransition>*@
}
