@namespace BlazorComponent
@inherits BDomComponentBase

@if (Show)
{
    <span class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref">
        <span class="@CssProvider.GetClass("content")">
            @ChildContent
            @if (Close)
            {
                <SlotComponent Description="SlotProvider.GetSlot<BIcon>()">
                    @CloseIcon
                </SlotComponent>
            }
        </span>
    </span>
}
