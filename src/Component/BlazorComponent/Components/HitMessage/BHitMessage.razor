@namespace BlazorComponent
@inherits BDomComponentBase

<div class="@CssProvider.GetClass()" id="@Id">
    <div class="@CssProvider.GetClass("wrap")">
        <div class="@CssProvider.GetClass("message")">
            @ChildContent
        </div>
    </div>
</div>