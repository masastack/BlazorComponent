@namespace BlazorComponent
@typeparam TItem
@typeparam TDataTable
@inherits ComponentAbstractBase<TDataTable>

<thead>
    <tr class="@CssProvider.GetClass("progress")">
        <th class="@CssProvider.GetClass("column")" @attributes="ColspanAttrs">
            <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BLoadableProgress<>))"></AbstractComponent>
        </th>
    </tr>
</thead>

