@namespace BlazorComponent
@inherits BDomComponentBase

<thead class="@CssProvider.GetClass()">
    <tr>
        @foreach (var header in Headers)
        {
            @RenderPart(typeof(BDataTableHeaderHeader<>),header)
        }
    </tr>
</thead>
