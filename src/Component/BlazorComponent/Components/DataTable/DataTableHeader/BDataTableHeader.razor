@namespace BlazorComponent
@inherits BDomComponentBase

@if (IsMobile)
{
    @RenderPart(typeof(BDataTableHeaderMobile<>), Headers)
}
else
{
    @RenderPart(typeof(BDataTableHeaderDesktop<>), Headers)
}
