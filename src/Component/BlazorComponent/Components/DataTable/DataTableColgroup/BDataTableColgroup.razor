@namespace BlazorComponent
@inherits BDomComponentBase
@typeparam TItem

<colgroup>
    @foreach (var header in Headers)
    {
        <BDataTableColGroupItem Header="@header"
                                CalculateRealWidth="@HasFixedColumn"
                                @key="header" />
    }
</colgroup>
