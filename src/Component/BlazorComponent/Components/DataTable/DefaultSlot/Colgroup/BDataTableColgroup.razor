@namespace BlazorComponent
@typeparam TItem
@typeparam TDataTable
@inherits ComponentPartBase<TDataTable>

<colgroup>
    @foreach (var header in ComputedHeaders)
    {
        <col class="@(header.Divider?"divider":null)" />
    }
</colgroup>

