@namespace BlazorComponent
@typeparam TItem
@typeparam TDataTable
@inherits ComponentPartBase<TDataTable>

@if (ItemContent != null)
{
    @RenderPart(typeof(BDataTableScopedRows<,>),Items)
}
else
{
    @RenderPart(typeof(BDataTableDefaultRows<,>),Items)
}

