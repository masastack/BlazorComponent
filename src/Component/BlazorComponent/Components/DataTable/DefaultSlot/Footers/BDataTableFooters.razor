@namespace BlazorComponent
@typeparam TItem
@typeparam TDataTable
@inherits ComponentPartBase<TDataTable>

@FooterContent
@if (!HideDefaultFooter)
{
    <BDataFooter @attributes="GetAttributes(typeof(BDataFooter))"></BDataFooter>
}

