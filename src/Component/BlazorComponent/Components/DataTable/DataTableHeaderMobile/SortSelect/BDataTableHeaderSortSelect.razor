@namespace BlazorComponent
@typeparam TDataTableHeader
@inherits ComponentPartBase<TDataTableHeader>

<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(ISelect<,,>))"
                   Items="@Items">
</AbstractComponent>
