@namespace BlazorComponent
@inherits BDomComponentBase

<thead class="@ConfigProvider.GetCss(typeof(BTableHeader))" id="@Id" @ref="Ref">
    <tr>
        @if (Headers != null)
        {
            @foreach (var header in Headers)
            {
                <th>@header</th>
            }
        }
    </tr>
</thead>
