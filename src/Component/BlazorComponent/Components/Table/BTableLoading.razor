@namespace BlazorComponent
@inherits BDomComponentBase

<thead class="@CssProvider.GetClass()" id="@Id" @ref="Ref">
    <tr class="@CssProvider.GetClass("progress")">
        <th class="@CssProvider.GetClass("column")" style="@CssProvider.GetStyle("column")">
            <AbstractComponent Metadata="AbstractProvider.GetMetadata<BProcessLinear>()"></AbstractComponent>
        </th>
    </tr>
</thead>
