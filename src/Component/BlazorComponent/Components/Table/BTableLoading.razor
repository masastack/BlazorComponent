@namespace BlazorComponent
@inherits BDomComponentBase

<thead class="@CssProvider.GetClass(this)" id="@Id" @ref="Ref">
    <tr class="@CssProvider.GetClass(this,"progress")">
        <th class="@CssProvider.GetClass(this,"column")" style="@CssProvider.GetStyle(this,"column")">
            <SlotComponent Description="SlotProvider.GetSlot<BProcessLinear>()"></SlotComponent>
        </th>
    </tr>
</thead>
