@namespace BlazorComponent
@typeparam TAlert
@inherits ComponentAbstractBase<TAlert>

<AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BButton>("dismissible"))" aria-label="@CloseLabel" Icon Small OnClick="@HandleOnDismiss">
    <AbstractComponent Metadata="@(AbstractProvider.GetMetadata<BIcon>("dismissible"))">@CloseIcon</AbstractComponent>
</AbstractComponent>