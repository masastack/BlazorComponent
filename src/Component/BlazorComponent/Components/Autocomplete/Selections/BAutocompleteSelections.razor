@namespace BlazorComponent
@typeparam TItem
@typeparam TItemValue
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

@if (HasSlot || Multiple)
{
    @RenderPart(typeof(BSelectSelections<,,,>),name:"base")
}
else
{
    @RenderPart(typeof(BTextFieldInput<,>))
}
