@namespace BlazorComponent
@typeparam TDatePickerHeader
@inherits ComponentPartBase<TDatePickerHeader>

@*TODO: arialLabel*@
<BButton @attributes="GetAttributes(typeof(BButton),Change)">
    <BIcon @attributes="GetAttributes(typeof(BIcon))">
        @((Change < 0) == !RTL ? PrevIcon : NextIcon)
    </BIcon>
</BButton>

