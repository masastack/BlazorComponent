@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref">
        <AbstractComponent Change="@(-1)" Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerHeaderBtn<>))">
        </AbstractComponent>
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerHeaderHeader<>))"></AbstractComponent>
        <AbstractComponent Change="@(+1)" Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerHeaderBtn<>))"></AbstractComponent>
    </div>
</BCascadingValue>

