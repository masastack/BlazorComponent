@namespace BlazorComponent
@inherits BDomComponentBase

@*TODO: onwheel event*@
<BCascadingValue Value="this">
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref">
        <Transition Name="@ComputedTransition">
            <GroupElement Tag="table" Key="TableDate">
                <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerTableSlot<>))"></AbstractComponent>
            </GroupElement>
        </Transition>
    </div>
</BCascadingValue>

