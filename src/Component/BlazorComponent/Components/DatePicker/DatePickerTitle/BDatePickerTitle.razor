@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" @ref="Ref">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerTitleYearBtn<>))"></AbstractComponent>
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDatePickerTitleTitleDate<>))"></AbstractComponent>
    </div>
</BCascadingValue>
