@namespace BlazorComponent
@typeparam TValue
@typeparam TInput
@inherits ComponentAbstractBase<TInput>

@if (HasCounter)
{
    if (CounterContent != null)
    {
        @CounterContent
    }
    else
    {
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BCounter))"></AbstractComponent>
    }
}