@namespace BlazorComponent
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

@if (HasCounter)
{
    if (CounterContent != null)
    {
        @CounterContent
    }
    else
    {
        <BCounter @attributes="GetAttributes(typeof(BCounter))"></BCounter>
    }
}