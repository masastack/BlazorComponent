@namespace BlazorComponent
@inherits BDomComponentBase

<Element Tag="@Tag" class="@CssProvider.GetClass()" style="@(GutterStyle + " " + CssProvider.GetStyle())" id="@Id"
         ReferenceCaptureAction="r => Ref = r"
         @attributes="Attributes">
    @ChildContent
</Element>