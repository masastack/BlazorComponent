@namespace BlazorComponent
@inherits BDomComponentBase

<div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref">
    <div @ref="@ToolbarRef" class="@ToolbarClass" style="@ToolbarStyle">
        @ToolbarContent
    </div>
    <div @ref="@ContentRef" class="@ContentClass" style="@ContentStyle">
        @EditorContent
    </div>
</div>
