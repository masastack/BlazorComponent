@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">
    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref" 
        @onclick="HandleOnClickAsync"
        @attributes="Attributes">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BStepperStepStep<>))"></AbstractComponent>
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BStepperStepLabel<>))"></AbstractComponent>
    </div>
</BCascadingValue>
