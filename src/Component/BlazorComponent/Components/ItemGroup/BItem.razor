@namespace BlazorComponent
@inherits BItemBase<BItemGroup>

@{
    var context = new ItemContext
    {
        IsActive = IsActive,
        Toggle = ToggleItem,
        Ref = RefBack
    };

    @ChildContent(context)
}