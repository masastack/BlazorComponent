@namespace BlazorComponent
@typeparam TIcon
@inherits ComponentPartBase<TIcon>

@if (OnClick.HasDelegate && !Disabled)
{
    @RenderPart(typeof(BButtonIconSlot<>))
}
else
{
    <Element Tag="@Tag" class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" ReferenceCaptureAction="_ => Ref = _" @attributes="Attrs">@NewChildren</Element>
}

