@namespace BlazorComponent
@inherits BDomComponentBase

@if (_tag == IconTag.I)
{
    if (Click.HasDelegate)
    {
        <button type="button"
                class="@CssProvider.GetClass()"
                style="@CssProvider.GetStyle()"
                id="@Id"
                @ref="Ref"
                @onclick="Click">
            @_icon
        </button>
    }
    else
    {
        <i class="@CssProvider.GetClass()"
           style="@CssProvider.GetStyle()"
           id="@Id"
           @ref="Ref"
           @onclick="Click">
            @_icon
        </i>
    }
}
else
{
    // Render SVG

    <span role="img"
          class="@CssProvider.GetClass()"
          style="@CssProvider.GetStyle()"
          id="@Id"
          @ref="Ref"
          @onclick="Click">
        @((MarkupString) _icon)
    </span>
}
