@namespace BlazorComponent
@typeparam TIcon
@inherits ComponentPartBase<TIcon>

<IfTransitionElement
    Tag="button"
    Value="@ComputedIfElse"
    type="button"
    class="@CssProvider.GetClass()"
    style="@CssProvider.GetStyle()"
    ReferenceCaptureAction="_ => Ref = _"
    @attributes="@Attrs">
    @NewChildren
</IfTransitionElement>