@namespace BlazorComponent
@typeparam TItem
@typeparam TValue
@typeparam TInput
@inherits BSelectMenu<TItem, TValue,TValue, TInput>

@Render(typeof(BMenu), props => {
    props.AddChildContent(@<CascadingValue Value="Component">
        @if (ComputedItems.Any())
    {
        <AbstractComponent Items="@ComputedItems" Metadata="AbstractProvider.GetMetadata(typeof(BCascaderMenuBody<,,>))">
        </AbstractComponent>
    }
    </CascadingValue>);
}, referenceCapture: menu => Menu=menu)
