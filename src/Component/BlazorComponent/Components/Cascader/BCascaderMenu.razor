@namespace BlazorComponent
@typeparam TItem
@typeparam TValue
@typeparam TInput
@inherits BSelectMenu<TItem, TValue,TValue, TInput>

<BMenu @attributes="GetAttributes(typeof(BMenu))" @ref="Menu">
    <CascadingValue Value="Component">
        @if (ComputedItems.Any())
        {
            @RenderPart(typeof(BCascaderMenuBody<,,>),ComputedItems, arg0Name:"Items")
        }
    </CascadingValue>
</BMenu>
