@namespace BlazorComponent
@typeparam TItem
@typeparam TValue
@typeparam TInput
@inherits ComponentPartBase<TInput>

<div class="@CssProvider.GetClass("lists")">
    <BCascaderList TItem="TItem"
                   TValue="TValue"
                   Items="ComputedItems"
                   @attributes="@GetAttributes(typeof(BCascaderList<,>))">
    </BCascaderList>
</div>
