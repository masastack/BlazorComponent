@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">

	<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BTooltipContent<>))"></AbstractComponent>

	<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BTooltipActivator<>))"></AbstractComponent>

	<Element Tag="@Tag" class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" ReferenceCaptureAction="elmentRference=> Ref=elmentRference">
	</Element>

</BCascadingValue>
