@namespace BlazorComponent
@inherits BMenuable

@RenderPart(typeof(BTooltipActivator<>))

<CascadingValue Value="FirstBoot" Name="FirstBoot">
    <Element Tag="@Tag"
             class="@CssProvider.GetClass()"
             style="@CssProvider.GetStyle()"
             id="@Id"
             ReferenceCaptureAction="r => Ref = r">
        @RenderPart(typeof(BTooltipContent<>))
    </Element>
</CascadingValue>
