@namespace BlazorComponent
@inherits BActivatable

<BCascadingValue Value="this">

    <div class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id" @ref="Ref" role="dialog">
    </div>

    <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDialogActivator<>))"></AbstractComponent>

    <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDialogContent<>))"></AbstractComponent>

</BCascadingValue>

<AbstractComponent Metadata="AbstractProvider.GetMetadata<BOverlay>()" @ref="Overlay"></AbstractComponent>