@namespace BlazorComponent
@typeparam TDialog
@inherits ComponentPartBase<TDialog>

<Transition Name="@Transition">
    <Element Show="Value" class="@CssProvider.GetClass("innerContent")" style="@CssProvider.GetStyle("innerContent")" ReferenceCaptureAction="elmentRference=> DialogRef=elmentRference">
        @ComponentChildContent
    </Element>
</Transition>
