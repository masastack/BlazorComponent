@namespace BlazorComponent
@typeparam TDialog
@inherits ComponentAbstractBase<TDialog>

@if (ShowContent)
{
    <div Show="Value" class="@CssProvider.GetClass("content")" style="@CssProvider.GetStyle("content")" @ref="ContentRef" @attributes="ContentAttrs">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BDialogInnerContent<>))"></AbstractComponent>
    </div>
}
