@namespace BlazorComponent
@typeparam TComponent
@inherits ComponentPartBase<TComponent>

<BButton Disabled="@IsDisabled"
         Icon
         Text
         OnClick="@OnClick"
         @attributes="GetAttributes(typeof(BButton))">
    <BIcon @attributes="GetAttributes(typeof(BIcon))">
        @Icon
    </BIcon>
</BButton>

