@namespace BlazorComponent
@typeparam TComponent
@inherits ComponentAbstractBase<TComponent>

<AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BButton))"
                   Disabled="@IsDisabled"
                   Icon="@true"
                   Text="@true"
                   OnClick="@OnClick">
    <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BIcon))">
        @Icon
    </AbstractComponent>
</AbstractComponent>

