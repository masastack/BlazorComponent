@namespace BlazorComponent
@typeparam TValue
@inherits BInputSlotBody<IInput>

<div class="@CssProvider.GetClass("radio-group")">
    @if (HasLabel)
    {
        <AbstractComponent Metadata="AbstractProvider.GetMetadata<BLabel>()">
            @if (LabelContent != null)
            {
                @LabelContent
            }
            else
            {
                @Label
            }
        </AbstractComponent>
    }
    @InputChildContent
</div>