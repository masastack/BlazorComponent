@namespace BlazorComponent
@inherits BDomComponentBase

<div class="@CssProvider.GetClass(this)" style="@CssProvider.GetStyle(this) @OriginalStyle" id="@Id" @ref="Ref" @attributes="Attributes">
    <div class="@CssProvider.GetClass(this,"wrap")">
        @ChildContent
    </div>
</div>