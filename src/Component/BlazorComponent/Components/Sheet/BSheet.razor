@namespace BlazorComponent
@inherits BDomComponentBase

<BCascadingValue Value="this">
    <Element Tag="@Tag" class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()" id="@Id"
             ReferenceCaptureAction="r => Ref = r"
             @oncontextmenu:preventDefault="OnContextmenuPreventDefault"
             @attributes="Attributes">
        <AbstractComponent Metadata="AbstractProvider.GetMetadata(typeof(BSheetBody<>))"></AbstractComponent>
    </Element>
</BCascadingValue>