@namespace BlazorComponent
@inherits BDomComponentBase

<Element Tag="@WrappedTag" class="@CssProvider.GetClass("wrapper")" style="@CssProvider.GetStyle("wrapper")">
    <Element Tag="@Tag" Class="@CssProvider.GetClass()" Style="@CssProvider.GetStyle()" @attributes="Attributes">
        @if (ChildContent != default)
        {
            @ChildContent((IsLast, IsDisabled))
        }
        else
        {
            @Text
        }
    </Element>
</Element>

<!--When using razor definition without `Items` parameter-->
@if (Breadcrumbs.ChildContent != null && !IsLast)
{
    <BBreadcrumbsDivider Divider="@Breadcrumbs.Divider" DividerContent="@Breadcrumbs.DividerContent" @attributes="GetAttributes(typeof(BBreadcrumbsDivider))"></BBreadcrumbsDivider>
}