@namespace BlazorComponent
@inherits BDomComponentBase

<Element Tag="@Tag" class="@CssProvider.GetClass()" style="@CssProvider.GetStyle()">
    @if (DividerContent != null)
    {
        @DividerContent
    }
    else
    {
        @Divider
    }
</Element>